# Render Infrastructure-as-Code Blueprint
# Deploy backend as a high-performance Node.js Web Service on Render
services:
  - type: web
    name: jurisaccess-backend
    runtime: node
    rootDir: backend
    plan: free
    region: oregon
    branch: main
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: LLM_PROVIDER
        value: gemini
      - key: GEMINI_API_KEY
        sync: false
      - key: LLM_TIMEOUT_MS
        value: "8000"
      - key: LLM_CIRCUIT_BREAKER_MS
        value: "60000"
      - key: CORS_ORIGIN
        sync: false
      - key: RATE_LIMIT_WINDOW_MS
        value: "60000"
      - key: RATE_LIMIT_MAX_REQUESTS
        value: "60"
